# full path to application database
APP_DB_PATH = 'app.db'

# full path to application log file
APP_LOG = 'app.log'

# full path to a file to collect all stderr message
ERR_LOG = 'stderr.log'

# port to listen on
PORT = 80
